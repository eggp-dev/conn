/**
 * The one place the films name the repository. If it moves (for example to an organization),
 * change this line and re-render; nothing else in media/demo spells the address out.
 * GitHub keeps redirecting the old address after a transfer, so already published films stay valid.
 */
export const repository = "github.com/eggp-dev/conn";

/** Agent clients Conn sets up today, as shown in the closing card. */
export const worksWith = ["Claude Code", "Codex", "Cursor", "GitHub Copilot"] as const;
