# Conn에 기여하기

[English](CONTRIBUTING.md) · 한국어 · [README](README.ko.md)

Conn은 초기 단계의 프로젝트입니다. 명확한 재현 절차, 꼼꼼한 플랫폼 테스트, 이해하기 쉬운 한 문장도 새로운 기능만큼 도움이 됩니다. **영어와 한국어** 이슈·PR을 모두 환영합니다.

## 처음 기여하기 좋은 작업

- 실제 macOS·Windows에서 실행, 입력, 크기 변경, 제어 요청, 거절, 제어권 회수, 탭 종료를 확인합니다.
- 셸·백엔드 호환성 문제를 최소 명령으로 재현해 제보합니다.
- 키보드 탐색, 줄바꿈, 포커스, 스크린 리더 라벨, 모션 줄이기 동작을 개선합니다.
- 영어·한국어 UI 문구와 문서를 함께 다듬습니다.
- 제어권, 정책, 요청 기록, 세션 수명 관련 버그의 회귀 테스트를 추가합니다.

큰 기능이나 새 백엔드를 제안할 때는 아키텍처를 변경하기 전에 사용자의 작업 흐름을 이슈로 설명해 주세요. 기존 이슈도 먼저 확인해 주세요. 보안 문제는 민감한 내용을 공개하지 말고 [SECURITY.md](SECURITY.md)를 따라 제보하세요.

## 개발 환경 준비

rustup, **Node.js 24**, 네이티브 데스크톱 개발을 위한 [Tauri 필수 구성 요소](https://v2.tauri.app/start/prerequisites/)를 설치합니다. Rust 버전은 `rust-toolchain.toml`에 고정되어 있습니다. CI는 릴리스 도구에 Python 3.12도 사용합니다.

```sh
git clone https://github.com/eggplantiny/conn.git
cd conn
```

포크로 기여한다면 자신의 포크를 복제하고 작업 범위가 명확한 브랜치를 만드세요. 로컬 프로필, 감사 로그, 셸 기록, 인증 정보, 빌드 결과, 개인 테스트 파일은 커밋에 넣지 않습니다.

### 데스크톱 실행

```sh
cd frontends/tauri
npm ci
npm run tauri dev
```

실행 스크립트가 Tauri 시작 전에 네이티브 CLI 보조 실행 파일을 빌드합니다. Rust 코어만 빌드할 수 있더라도 네이티브 데스크톱 빌드에는 운영체제별 라이브러리가 필요합니다.

### 공유 UI를 브라우저에서 실행

`frontends/tauri`에서:

```sh
npm ci
npm run test:browser
```

`http://127.0.0.1:1421/`을 엽니다. 로컬 WebSocket 어댑터를 통해 **같은 App과 Rust 프런트엔드 하네스**를 사용합니다. 별도 웹 제품이나 가짜 셸이 아니라 개발 보조 도구입니다. 명령은 로컬 사용자 권한으로 실행됩니다. 실행 스크립트가 별도 설정 폴더와 에이전트 연결 주소를 출력합니다.

브라우저 새로고침·연결 종료는 실행 중인 세션을 끝냅니다. 버려도 되는 파일로 시험하고 필요한 때만 새로고침하세요. 수명과 연결 주소에 대한 내용은 [브라우저 테스트](docs/browser-testing.md)를 참고하세요.

## 변경할 위치 찾기

| 경로 | 역할 |
|---|---|
| `crates/core` | PTY, 세션 제어, 정책, 감사, IPC, 셸·백엔드 프로필 |
| `crates/cli` | 터미널 프런트엔드, headless serve, MCP 어댑터, 사람·에이전트 CLI |
| `crates/frontend` | 공유 네이티브 프런트엔드 하네스와 명령 처리 |
| `crates/browser-harness` | 공유 하네스에 연결하는 로컬 브라우저 테스트 전송 계층 |
| `frontends/tauri/src` | 공유 Svelte UI, 터미널 렌더러, 타임라인, 설정, i18n |
| `frontends/tauri/src-tauri` | Tauri 어댑터, 네이티브 설정, 패키징 |
| `plugin` | 에이전트 작업 절차와 MCP 서버 정의 |
| `docs` | 사용법, 프로토콜, 아키텍처, 검증, 릴리스 |

계약을 변경하기 전에 관련 [아키텍처](docs/architecture.md), [프로토콜](docs/protocol.md), [정책](docs/policy.md), [백엔드](docs/backends.md) 문서를 읽어 주세요.

## 협업의 기본 동작 유지

- 사람의 입력이 에이전트 입력보다 우선합니다. 제어권 승인이 모든 명령 승인을 뜻하지는 않습니다.
- 요청 메타데이터와 실행 증거를 구분합니다. 거절된 요청에 예정 명령이 있더라도 실행한 것처럼 보이면 안 됩니다.
- 요청 원문에는 실제 메서드와 인자를 보존합니다. 이유 문장이나 나중에 실행한 명령으로 없는 원문을 추정하지 않습니다.
- 정책 차단, 사용자의 거절, 만료, 취소는 서로 다릅니다. 이벤트, 저장, UI 전체에서 구분합니다.
- 프로필은 설정에서 관리합니다. 새 탭은 저장된 기본값을 사용하고 기존 세션은 현재 구성을 유지합니다.
- 하나의 공유 UI와 네이티브 엔진을 유지합니다. 브라우저 테스트는 전송 계층만 바꾸며 동작을 따로 구현하지 않습니다.
- 원격 대상의 파일 경로를 호스트 파일 시스템에서 해석하지 않습니다. 백엔드 변경에는 인자 구성, 프로세스 수명, 정책 경계 테스트가 필요합니다.
- UI 문구는 기존 i18n 키를 사용하고 새 키에는 영어·한국어를 함께 추가합니다. 컴포넌트에 사용자용 문구를 직접 넣지 않습니다.
- 색상과 애니메이션 없이도 상태를 이해할 수 있어야 합니다. 모션 변경 시 키보드 포커스와 `prefers-reduced-motion`을 확인합니다.

## 변경 검증

저장소 루트에서:

```sh
cargo test --workspace --locked
```

`frontends/tauri`에서:

```sh
npm ci
npm test
npm run build
```

네이티브 어댑터·패키징을 변경했다면 해당 플랫폼의 `frontends/tauri`에서 다음도 실행합니다.

```sh
npm run tauri build -- --debug --no-bundle
```

UI 변경은 네이티브 앱 또는 공유 브라우저 환경에서 실제 흐름을 확인하세요. 레이아웃·모션이 관련되면 스크린샷을 첨부합니다. 승인 관련 UI를 변경한다면 승인, 거절, 취소도 확인합니다. 구현 내용을 그대로 반복하는 테스트보다 동작의 회귀를 잡는 테스트를 추가해 주세요.

**어떤 플랫폼에서 무엇을 확인했는지** 구체적으로 적어 주세요. 컴파일, 자동 테스트, 설치 파일 생성, 실제 네이티브 조작은 서로 다른 증거입니다. 크로스 컴파일만으로 해당 플랫폼 동작이 검증됐다고 표현하지 않습니다.

제출 전 확인:

```sh
git diff --check
git status --short
```

스테이징한 파일을 모두 확인하고 특히 로그, 스크린샷, 연결 주소, 로컬 설정을 살펴보세요. 진단 자료에 민감한 내용이 있으면 첨부 전에 가립니다.

## 검토하기 좋은 PR 작성

구체적인 문제, 변경 후 동작, 검증 방법을 설명합니다. 짧은 전후 예시가 도움이 됩니다.

> 제어 요청을 거절한 뒤 새로고침하면 타임라인에서 요청 명령이 사라졌습니다. 요청 원문을 결정 결과와 함께 저장하고 상세 화면에서 복원하도록 변경했습니다. 거절·새로고침 흐름과 원문이 없는 과거 기록을 확인했습니다.

실행한 관련 명령과 결과, UI 스크린샷, 남은 플랫폼 검증을 적어 주세요. 사용법이 바뀌면 두 언어의 문서를 함께 업데이트합니다. 관련 없는 전체 포맷 변경이나 생성 파일 변경은 피합니다.

별도 기여자 라이선스 동의서(CLA)는 없습니다. 기여는 저장소의 [MIT 라이선스](LICENSE)에 따릅니다.

## 릴리스

기여자에게 서명 인증 정보나 릴리스 권한은 필요하지 않습니다. CI가 변경을 검증하며 릴리스 준비와 공개는 [릴리스 안내](docs/releasing.ko.md)의 관리자 절차를 따릅니다. 일반 PR 작업에 비밀 정보 추가, 패키지 공개, 운영 도메인 변경을 포함하지 않습니다.
