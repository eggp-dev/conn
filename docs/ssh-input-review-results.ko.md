# SSH 입력과 승인 수정 결과

[English](ssh-input-review-results.md) · [테스트 재현 방법](browser-testing.md#ssh-input-cursor-and-approval-regression)

2026-09-22 · v0.8.4 (`06143d0`) 기준 v0.8.5 패치.

## v0.8.5 이후 터미널 크기 후속 조사 — 미배포

사용자는 업데이트·재시작 후에도 문제가 남고, 창 크기를 바꾸지 않은 상태에서 알림·승인 바의
모션이 나타날 때 가로와 세로가 모두 제한되어 보인다고 설명했다. 창이 고정이어도 승인 바가
열리고 닫히면 터미널에 남는 높이는 변한다. 이전 설정 모달 수정만으로 이 보고를 닫지 않는다.
사용자의 실제 중간 SSH 접속 구현은 아직 검증하지 않았다.

### 재현한 결함과 변경

- 브라우저 서버는 명령을 직렬 처리하지만 네이티브 Tauri는 별도 작업으로 실행한다.
  첫 크기 변경을 350ms 지연하자 화면이 104열로 돌아온 뒤에도 PTY는 83열로 남았다.
  별도 검사에서는 137열 대 83열도 확인했다.
- 창을 860×640으로 고정하고 승인 바의 높이 축소 전달을 500ms 늦추자, 화면은 38행으로
  돌아왔지만 PTY는 27행으로 남았고 커서는 37행 대 26행으로 어긋났다(0부터 센 행 번호).
  공통 UI·실제 Rust·SSH 경로에 지연을 주입한 재현이며 네이티브 타이밍을 직접 캡처한 것은 아니다.
- 크기 요청 직렬화만으로는 부족했다. 더 긴 승인 대기 명령을 빠르게 거부하자 두 그리드가
  모두 38행인데도 사용자 커서는 23행, 코어 커서는 37행인 두 번째 결함을 재현했다.
  셸의 화면 재출력과 렌더러 크기 변경이 서로 다른 순서로 적용됐다.
- 프런트엔드는 터미널마다 크기 요청 하나의 완료를 기다리고 가장 최근 대기 크기를 유지한다.
  백엔드는 출력과 같은 세션 잠금 안에서 적용 크기를 출력 프레임에 담고, 출력이 없는 크기 변경도
  빈 프레임으로 전달한다. 렌더러는 이 순서대로 크기를 반영하며 이전 xterm 출력의 처리가 끝난
  뒤 다음 프레임을 적용한다. 화면 영역 측정은 크기를 요청하고, 백엔드보다 먼저 렌더러를 바꾸지 않는다.
- 승인 바의 모션은 유지한다. 새 셸·원격 훅·숨겨진 입력·접근 조건·권한은 추가하지 않는다.

### 검증과 남은 범위

- 최종 수정 후 가로 지연 검사와 고정 창 승인 검사가 통과했다. 그리드·커서 일치와 사용 가능한
  전체 영역의 크기를 확인했다. 일반 고정 창 검사는 실제 터미널 모션을 기록하고, 지연 검사는
  늦은 크기 변경이 도착하기 전에 승인 바를 닫는다.
- Linux Chromium·실제 SSH 하네스 전체가 통과했다. 240~7,000자 입력, 유니코드, 설정,
  긴 승인, 세션 허용, 사람의 커서 편집·제어권 회수와 로컬 셸 복귀를 포함한다.
  화면 배율 1·1.25·1.5·2배에서도 영역 채움을 확인했다. 이는 Mac의 실제 다중 모니터 검증이 아니다.
- Linux WebKit 26.6에서 커서를 맨 아래로 내린 뒤 고정 창 승인 열기·거부 18회를 통과했다.
  실제 터미널 모션, 짧은 명령·줄바꿈된 명령, 빠른 닫기를 포함한다. 최종 실행에도 수정 전과 같은
  ResizeObserver 경고가 한 번 있었으나 지속되는 그리드·커서 불일치는 없었다.
  오류 없는 콘솔 실행이나 네이티브 macOS 검증으로 기록하지 않는다.
- 협업 맥락 하네스 12단계도 통과했다. 백그라운드 탭, 준비 요청, 비공유 터미널 생성과
  공유 전환을 포함한다.
- 기본 Rust 테스트 259개(선택적 SSH 1개 제외), 프런트엔드 55개와 프런트엔드 빌드가 통과했다.
  이 Linux 호스트에는 Pango 개발 패키지가 없어 데스크톱 번들은 빌드하지 못했다.
- 앞선 직접 OpenSSH와 중첩 대화형 SSH 검사는 각각 세 가지 너비에서 입력·승인 18회를
  통과했다. 사용자의 실제 중간 접속 구현이 올바르다는 근거로 확대하지 않는다.

이 후속 수정은 아직 미배포이며 공개된 v0.8.5에는 포함되지 않았다. 사진과 정확히 같은 깨짐은 사용자의
설치된 macOS 앱·SSH 환경에서 확인해야 한다. 크기·출력 검사가 네이티브 키보드 입력 순서나
물리 키보드 IME 동작까지 검증하는 것은 아니다. 별도 `/bin/sh` canonical 입력 실험에서
승인 거절 후 이전 줄바꿈 글자가 남은 항목도 미해결이며 사진의 원인으로 확정하지 않았다.

### PR #64 네이티브 macOS 검증

2026-09-22, 별도 체크아웃의 `codex/terminal-resize-order` HEAD가
`e97c2048e320d6c09fa1d556bd343c52deab0b58`임을 확인하고 검사했다.
[해당 커밋의 CI](https://github.com/eggp-dev/conn/actions/runs/35705822838)는 모두 성공했다.
Rust는 Linux·macOS·Windows, 데스크톱 CI는 Linux에서 실행됐다. 아래 Mac 앱 검증은 별도 결과다.
[집계 증거](validation/macos-resize-order-results.json)에는 개인 경로나 원시 출력이 없다.

**환경·실행 확인:** macOS 27.0 (`26A428`), arm64, Node.js 24.13.0, Rust 1.95.0.
루트 `npm ci`, Rust 259개(선택적 SSH 1개 제외), 프런트엔드 55개, 프런트엔드 빌드가 통과했다.
`frontends/tauri`에서 `npm run tauri -- build --debug --bundles app --config <임시 설정>`으로
ad-hoc 서명 네이티브 번들을 만들었다. 앱 이름은 `Conn Resize Validation`, 식별자는
`dev.eggp.conn.resizevalidation`이며 `CONN_CONFIG_DIR`와 `CONN_SOCKET`을 분리했다.
실행 파일은 새 체크아웃 아래
`frontends/tauri/src-tauri/target/debug/bundle/macos/Conn Resize Validation.app/Contents/MacOS/conn-desktop`이고,
PID `34233`이 테스트 소켓을 소유했다. 바이너리 SHA-256은
`b199fa89ef6bf141b4c30e989d620d1e51fae5312c712b57d4e4ea813621c139`이다.
표시 버전 0.8.5만으로 설치된 앱을 식별하지 않았다. 실제 `tauri://localhost` WebKit과 Tauri IPC를 사용했다.

**SSH·측정 방법:** 임시 키와 고정 호스트 키를 사용하는 별도 루프백 OpenSSH에 기존 Conn PTY로
접속했다. 실제 원격 셸은 `/bin/bash --noprofile --norc -i`, Bash `3.2.57(1)-release`,
`TERM=xterm-256color`, `LC_ALL=en_US.UTF-8`였다. 전후 `stty size`는 `41 137`이었다.
이 결과는 Bash의 줄 편집 경로이며 별도 `/bin/sh` canonical 입력 결함의 해결 근거가 아니다.
네이티브 캡처와 접근성 트리를 확인하고, WebKit 검사기에 임시로 연결한 측정기로 실제 표시 DOM의
전체 행·커서 위치·영역·모션을 로컬에 기록해 에이전트 스냅샷과 대조했다.
빠른 닫기 12회는 카드가 나타난 뒤 80~82ms에 실제 DOM 거부 버튼을 클릭하도록 제한한 임시 UI
검사로 실행했다. 제품 전송 계층·크기 요청·출력에는 지연이나 대체 구현을 넣지 않았고,
측정 중에는 검사기를 닫았다. 두 임시 검사 장치는 실행 후 제거했다.

| 실기기 항목 | 결과 |
| --- | --- |
| 고정 창 승인 바 | 창 1120×720, WebKit 내용 1120×688을 고정했다. 충분한 출력으로 커서를 맨 아래에 둔 뒤 짧은 명령·478~1,678자 명령을 검사했다. 승인 바 열기·승인·거부·사람의 중단 총 18회에 빠른 거부 12회를 포함했다. |
| 실제 모션과 복귀 | 빠른 반복에서 창 크기는 동일했고 302개 프레임에 실제 터미널 이동이 기록됐다. 137열을 유지하며 승인 카드 높이에 따라 41→30·26·20행으로 줄었다가 복귀했다. 사람에게 제어권을 돌려주는 바가 보일 때는 정상적으로 38행이었다. 최종 터미널은 사용 가능한 영역을 채웠다. |
| 화면·코어 비교 | 승인 전후, 편집, 출력과 셸 복귀의 38개 지점에서 전체 표시 행·커서·행 수가 일치했다. 이 수치는 네이티브 DOM 비교이며 기존 브라우저 하네스나 파서 테스트 수치가 아니다. |
| 사람의 입력 | 1,221자 영문·한글·이모지 명령에서 좌우 이동·중간 삽입·Backspace·Enter를 조작하고 `수정END>` 출력을 확인했다. 첫 방향키가 `human_input`으로 제어권을 회수했고 다음 에이전트 쓰기는 `not_controller`였다. |
| 명령 정책 | 일반 `printf`·`touch`는 자동 실행됐다. **세션 허용**으로 임시 파일 하나를 삭제한 뒤 같은 규칙의 두 번째 파일은 자동 삭제됐다. `recursive delete`·`privilege escalation`은 계속 승인을 요구했다. 금지 규칙은 위험 문자열을 인자로 담은 무해한 `printf`로 확인했다. 실제 삭제는 합성 파일 두 개뿐이다. |
| 승인 중 사람 입력 | Ctrl-C는 권한 상승 요청을 실행하지 않고 `denied (human_input)`으로 정리하며 제어권을 회수했다. 이를 프로토콜의 cancelled 상태로 기록하지 않는다. |
| 셸 유지 | 원격 Bash PID는 전후 `34307`로 같았고 SSH 종료 후 원래 로컬 Bash PID `34253`으로 돌아왔다. 같은 화면 식별자를 유지했다. |
| 개행 구분 | 짧은 JSON과 줄바꿈되는 JSON의 개행 없음·LF·CRLF 6건이 일치했다. 추가로 정확히 411자=137열×3인 출력 3건도 비교했다. 이 경계에서는 개행이 없어도 다음 프롬프트가 자동 줄바꿈된다. 임의 개행은 넣지 않았다. |

**남은 범위:** 빠른 거부 12회에서 `ResizeObserver loop completed with undelivered notifications`
경고 24건이 기록됐다. 측정 시작 후 전체 42건이며, 검사기에 이전 경고까지 합쳐 46건이 표시됐다.
확인한 모션 종료 지점에서 지속되는 그리드·커서 불일치는 없었지만 경고 자체는 해결하지 않았다.
물리 키보드 한글 IME, 사용자의 실제 원격 계정·중간 접속 구현, 별도 canonical `/bin/sh` 현상은
미검증 또는 미해결이다. 실행 중인 사용자 Conn/SSH 세션이 없어 실제 계정 인증은 시도하지 않았다.
확인한 입력·크기 경로에서 추가 제품 수정이 필요한 실패는 재현되지 않아 제품 코드는 바꾸지 않았다.
개인 경로·인증 키·원시 소켓/DOM/감사 로그·캡처는 커밋하지 않았다. 서명·공증된 설치 앱의
업데이트나 공개 배포 검증은 아니며, 버전은 0.8.5, 변경 내역은 Unreleased로 유지한다.

## 추가 보고: JSON 뒤에 바로 붙는 프롬프트

추가 사진에는 JSON의 닫는 괄호 바로 뒤에 색상이 있는 `[DEV]` 프롬프트가 붙어 있다.
출력 끝에 개행이 없을 때와 같은 모양이지만, 해당 명령의 원본 응답 바이트를 확보하지 못했으므로
사진만으로 원인을 확정하지 않는다.

현재 공통 UI·Rust·실제 SSH 하네스에서 짧은 JSON과 줄바꿈되는 긴 JSON 각각에 개행 없음·LF·CRLF를
적용한 6개 비교가 통과했다. 개행이 없으면 프롬프트가 JSON 뒤 같은 줄에 붙고, LF·CRLF가 있으면
다음 줄에서 시작했다. 6개 모두 사용자 화면의 행·커서와 코어가 일치했다. Conn을 거치지 않는
일반 Bash와 `/bin/sh` PTY도 세 경우가 같은 동작이었다. 사용자의 실제 응답 서버나 설치된 Mac 앱을
검증한 것이 아니라 같은 출력 바이트를 구성한 비교다.

두 동작을 구분하는 회귀 검사를 추가했다. Conn에서 임의로 개행을 끼워 넣으면 실제 셸·TUI의
커서 동작을 바꾸므로 그렇게 보정하지 않는다. 개행 없는 본문을 반환하는 명령 뒤에
`; printf '\n'`을 명시하면 다음 프롬프트를 분리할 수 있다. 반대되는 원본 바이트 증거가 없는
현재로서는 위에서 재현한 크기·출력 순서 결함과 구분해서 기록한다.

## 사용자 보고와 재현한 원인

사용자는 macOS에서 SSH 원격 셸을 사용하며, 긴 명령 이후 커서·입력 위치가 깨지고 **세션 허용**이 반응하지 않으며 위험 명령만 승인받는 자동 실행을 찾기 어렵다고 보고했다. 원하는 동작은 일반 명령 자동 실행, 위험 명령 승인이다.

- **승인:** SSH가 모든 명령을 개별 검토로 강제해 Autopilot에서도 세션 허용을 막았다. UI는 이유 없이 비활성화한 버튼을 표시했다.
- **입력·커서:** 설정은 모달로 바뀌었지만 예전 사이드 패널용 600픽셀 너비 축소가 남아 있었다. 설정을 열고 닫는 애니메이션 동안 실제 PTY가 반복해서 변경됐다. 실제 SSH의 긴 입력에서 화면 깨짐과 사용자 화면·코어 그리드 불일치를 재현했다.
- **긴 승인 내용:** 약 5,000자 명령이 승인 버튼을 화면 아래로 밀어냈다. 공통 카드에서 내용만 스크롤하고 동작 버튼은 유지하도록 수정했다.

## 변경 후 동작

POSIX SSH는 호스트의 cwd·홈·파일 경로를 검사하지 않고 기존 명령 정책을 적용한다. Autopilot에서 일반 명령은 실행하고 확인 규칙은 승인을 요청하며 금지 규칙은 계속 차단한다. 세션 허용은 해당 확인 규칙 이름에만 적용하고 UI에 즉시 알린다. Co-pilot, 연결 승인, 공유, 제어권 부여는 별도 책임을 유지한다.

SSH 프로필, 직접 실행한 SSH, Bash/Zsh 통합이 식별한 단일 SSH 명령에 적용한다. 해당 SSH 종료가 확인되면 로컬 분석으로 돌아간다. SSH 실행 상태 추적이 끊기면 개별 검토로 전환한다. 다른 원격 백엔드·비POSIX 셸·식별하지 못한 프로그램은 명령별 검토를 유지하고 UI에서 이유를 설명한다.

모달 설정을 열어도 뒤쪽 PTY 크기를 바꾸지 않는다. 실제 창과 협업 패널 크기 변경은 그대로 반영한다. 셸 교체, 원격 훅, 별도 실행 경로는 추가하지 않았다.

## 검증과 한계

- 기본 Rust 테스트 258개 통과. 기존 선택 실행 SSH 테스트는 이 수치에서 제외했다.
- 별도 실제 OpenSSH 인증·공유 테스트 통과. 원격 일반 명령 실행과 권한 상승 명령의 승인 대기를 확인했고, 권한 상승 명령은 실행하지 않았다. 비공유 인증정보는 공유 데이터에 남지 않았다.
- 프런트엔드 48개, 데스크톱 프런트엔드 빌드, 코어/xterm 화면·커서 비교 85회, 사이트 빌드, 저장소·문서 검사 통과.
- 공통 UI·실제 Rust·루프백 SSH 하네스에서 240~7,000자 입력, 한글·이모지, 반복 설정 열기·닫기, 작은 창의 긴 명령 승인, 세션 허용 범위, 사람의 커서 이동·중간 삽입·제어권 회수, 원래 로컬 셸 복귀를 검사했다.
- 기존 협업 맥락 하네스는 탭·알림·공유·재접속·셸 준비 12단계를 검증한다.

위 브라우저 검증 환경은 Linux Chromium과 UTF-8 원격 Bash다. 아래 macOS 네이티브 검증은 별도 실행 결과다. 사용자 증상의 모든 원인이 해결됐다고 단정하지 않으며, 배포 파일 검증은 별도로 기록한다.

## macOS 네이티브 후속 검증

2026-09-22, `codex/terminal-input-review`의 `ed57aac936bec98e26f0a69ac06241cef36c5834`를 Apple Silicon Mac에서 확인했다. 작업 폴더를 새로 복제했고 기존 로컬 변경은 없었다. [해당 커밋의 CI](https://github.com/eggp-dev/conn/actions/runs/35691004269)는 Linux·macOS·Windows의 Rust/데스크톱, 문서·프런트엔드, 협업 브라우저와 필수 검사 모두 성공했다. PR #63은 Draft 상태였다.

### 실행 환경과 증거 범위

- macOS 27.0 (`26A428`), arm64, Node.js 24.13.0, Rust 1.95.0. 루트 `npm ci` 완료.
- `frontends/tauri`에서 `npm run tauri dev`로 네이티브 실행을 확인했다. UI 조작 도구가 번들 없는 개발 프로세스를 선택하지 못해, 최종 조작은 같은 소스의 `npm run tauri build -- --debug --bundles app --config <임시 설정>`으로 만든 ad-hoc 서명 디버그 번들에서 진행했다. 임시로 감싼 개발 바이너리는 최종 검증 대상이 아니다.
- 테스트용 앱 이름·식별자, `CONN_CONFIG_DIR`, `CONN_SOCKET`을 분리하고 실행 바이너리 경로와 소켓 소유 PID를 확인했다. WebKit의 실제 `tauri://localhost` UI와 Tauri IPC를 사용했으며 브라우저 하네스로 대체하지 않았다.
- 별도 루프백 OpenSSH 서버, 임시 키·고정 호스트 키, 실제 `/bin/bash --noprofile --norc -i`, `en_US.UTF-8`, `xterm-256color`. SSH 명령은 Conn의 기존 로컬 PTY에서 입력했다. 실제 삭제 대상은 이번 실행에서 만든 임시 파일 두 개뿐이다.
- Mac에서 Rust 258개 통과·기존 선택 실행 SSH 테스트 1개 제외, 프런트엔드 48개 통과, 프런트엔드·네이티브 디버그 번들 빌드 통과, 코어/xterm 비교 85회 통과. 마지막 비교는 자동 파서 검사이며 네이티브 DOM 전체 비교 횟수가 아니다.

### 직접 확인한 동작

| 항목 | 결과 |
| --- | --- |
| 일반 명령 자동 실행 | 오른쪽 위 제어 상태에서 Autopilot을 선택했다. 연결 허용과 제어권 승인은 각각 별도로 진행했고 원격 `printf`, 임시 파일 `touch`는 명령 승인 없이 `executed`를 반환했다. |
| 긴 영문 입력과 설정 | 1,521자·7,029자 입력에서 설정 열기 전후 PTY 크기, 화면, 커서, 출력 순번이 동일했다. 반복 열기·닫기 후에도 입력을 유지했고 7,000자 본문 출력과 프롬프트 복귀를 확인했다. 설정을 연 상태에서도 같은 에이전트가 실행할 수 있었다. |
| 한글·이모지와 실제 창 크기 | `한글/경로/🙂/` 65회 반복을 붙여넣었다. 설정 열기·닫기와 1120×720 → 757×433 → 원래 크기 변경을 확인했다. 137열에서 90열로 바뀐 화면의 커서 위치를 코어 스냅샷과 대조했다. |
| 사람의 편집·제어권 회수 | 영문·한글 긴 명령에서 좌우 이동, 중간 삽입, Backspace, Enter를 조작해 `EDITEND>`·`수정END>` 출력을 확인했다. 에이전트 제어 중 첫 방향키 입력은 `human_input`으로 제어권을 회수했고 이후 에이전트 쓰기는 `not_controller`로 거절됐다. |
| 긴 승인 카드 | 5,109자 삭제 명령은 실행되지 않고 `delete files` 승인 대기가 됐다. 757×433 창에서 내용은 카드 안에서 스크롤되고 승인·거부·세션 허용 버튼이 보였다. **세션 허용** 클릭 후 `granted`와 임시 파일 삭제를 확인했다. |
| 허용 범위와 철회 | 다음 임시 파일 삭제는 승인 없이 실행됐다. `recursive delete`와 `privilege escalation`은 계속 승인 대기했고 UI에서 거부했다. 설정의 허용 목록에는 `delete files`만 표시됐다. 이를 철회하자 같은 규칙도 다시 승인 대기가 됐다. 한국어 UI에서 개별 승인도 확인했다. |
| 금지 규칙 | 정책 테스트의 루트 삭제 명령은 `deny`였다. 실제 에이전트 Enter 경로는 위험 문자열을 인자로만 담은 무해한 `printf '%s\n' 'rm -rf / '`로 검사해 `delete root`·`denied`와 정책 차단 타임라인을 확인했다. 실제 루트 삭제 명령은 실행 경로에 제출하지 않았다. |
| 승인 중 사람 입력 | Ctrl-C가 대기 명령을 실행하지 않고 `denied (human_input)`으로 정리하며 제어권을 회수했다. 이후 사람의 명령으로 동일 원격 Bash PID를 확인했다. |
| 원래 셸 복귀 | SSH 종료 후 처음의 로컬 Bash PID와 동일한 PID를 확인했다. 세션 화면 식별자도 유지됐다. |

네이티브 화면은 접근성 트리와 창 캡처로 확인하고, 선택한 화면·커서 위치는 에이전트 스냅샷과 대조했다. 모든 네이티브 행을 자동 비교한 검증은 아니다. 소켓 응답, 스냅샷, 감사 기록과 빌드 로그는 Mac의 임시 검증 폴더에 보관했으며 개인 경로·키·원시 로그·캡처는 저장소에 추가하지 않았다. 추가 제품 코드 변경은 하지 않았다.

### 남은 확인과 관찰

- **실제 키보드 한글 IME 조합은 미검증이다.** 자동 키 입력에서는 입력란의 `한글`과 PTY의 초성이 달랐다. 임시 WebKit 이벤트 추적에는 `compositionstart/update/end`가 없고 `isComposing=false`인 `insertText`/`insertReplacementText`와 키 이벤트가 기록됐다. 이 전달 방식으로 실제 키보드 조합의 성공·실패를 판정할 수 없어 제품 우회 코드를 넣지 않았다. 한글·이모지 붙여넣기와 커서 편집 통과를 IME 통과로 확대하지 않는다. 진단용 추적은 제거했다.
- WebKit 검사기에 크기·패널 변경 뒤 `ResizeObserver loop completed with undelivered notifications` 메시지가 누적돼 있었다. 확인한 화면·커서·편집 단계에서 지속되는 불일치는 재현되지 않았으나, 이를 오류 없는 콘솔 실행으로 기록하지 않는다.
- 사용자의 실제 SSH 서버·셸 설정, 비UTF-8 줄 편집기, 다른 macOS 버전, 서명·공증된 배포 앱의 설치·업데이트는 별도 확인이 필요하다. 이 네이티브 검증 당시 버전은 0.8.4, 변경 내역은 Unreleased였다. 해당 검증 자체는 머지·태그·릴리스가 아니다.
